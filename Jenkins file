Jenkinsfile (Declarative Pipeline)
pipeline {
    agent {
        any
    }
    stages {
        stage('pull code') {
            steps {
                sh 'node --version'
            }
        }
    }
}
