Jenkinsfile (Declarative Pipeline)
pipeline {
    agent {
        any
    }
    stages {
        stage('pull code') {
            steps {
                git credentialsId: 'user', url: 'https://github.com/saurabh-786-user/aws-sample-cicd-cdk-1.git'
            }
        stage {
            steps {
                sh 'docker build -t apache:v1 .'
                sh 'docker-compose up'
            }    
        }
    }
}
